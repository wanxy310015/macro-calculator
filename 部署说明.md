# 增肌减脂计算器 · 部署与访问方式

页面本体就是一个 `index.html` 文件，无任何后端依赖。以下是三种访问方式，按需选用。

## 方式一：双击打开（最简单）
直接双击 `index.html`，仅在本机浏览器使用。历史记录保存在本机浏览器 localStorage。

## 方式二：局域网访问（同一 WiFi 下的手机/平板）
1. 在本机启动服务器：`python -m http.server 8765`（在网页文件夹内执行）
2. 其他设备访问：`http://192.168.0.137:8765/index.html`（IP 以 `ipconfig` 实际为准）
3. 若手机打不开，在 Windows 防火墙放行 Python，或运行：
   `netsh advfirewall firewall add rule name="HTTP 8765" dir=in action=allow protocol=TCP localport=8765`（需管理员）

## 方式三：公网共享（任何网络下的任何设备，免注册）
双击 **`一键公网共享.bat`**，等约 10 秒，在窗口日志里找到形如
`https://xxxx-yyyy-yyyy.trycloudflare.com` 的网址，任何设备访问
`该网址/index.html` 即可。

原理：Cloudflare trycloudflare 免费快速隧道，把本机 8765 端口映射为公网 HTTPS。
限制（务必知道）：
- 你的电脑开机且脚本运行时才可访问；关窗口即下线
- 每次重启网址会随机变化
- 数据只在访问者自己的浏览器里，服务器不存任何数据

## 方式四：GitHub Pages（永久固定网址，需要你的 GitHub 账号，暂缓）
若以后想要永久网址，步骤：
1. 在 github.com 登录后新建一个公开仓库（如 `macro-calc`）
2. 在网页文件夹执行：
   ```
   git init
   git add index.html
   git commit -m "增肌减脂计算器 v1"
   git branch -M main
   git remote add origin https://github.com/<你的用户名>/macro-calc.git
   git push -u origin main
   ```
   （推送时浏览器会弹出 GitHub 登录授权）
3. 仓库 Settings → Pages → Source 选 `main` 分支 → Save
4. 一分钟后访问 `https://<你的用户名>.github.io/macro-calc/`

以后改版：改完 index.html 后 `git add . && git commit -m "更新" && git push` 即可。
